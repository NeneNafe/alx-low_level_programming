#include "main.h"

/**
 * main - a program that prints itself
 * @argc: argument count parameter
 * @argv: argument vector
 * Return: Always Success
 */

int main(int argc, char *argv[])
{
	printf("%s\n", *argv);
	(void)argc;
	return (0);
}
